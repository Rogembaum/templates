//Jenkinsfile (Declarative Pipeline)
pipeline {
    agent { sudo docker { image 'maven:3.3.3' } }
    stages {
        stage('build') {
            steps {
                sh 'mvn --version'
            }
        }
    }
}
