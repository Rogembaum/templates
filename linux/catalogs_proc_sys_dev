/proc   текущие процессы и параметры ядра
        /proc/cpuinfo — информация о процессоре.
        /proc/meminfo — информация о памяти.
        /proc/[PID]/ — информация о конкретном процессе.
        /proc/sys/ — дерево параметров ядра (тюнинг через sysctl).
        /proc/[PID]/oom_score — приоритет процесса для OOM killer.
        ; cat /proc/*/oom_score 2>/dev/null | sort -nr | head -n 1
        /proc/partitions — информация о разделах диска.
        /proc/net/ — информация о сетевых интерфейсах и соединениях.

        ; Защитить важный процесс от OOM:
        echo -1000 > /proc/$(pidof my_critical_service)/oom_score_adj

        /proc/kcore — информация о ядре Linux.

        🧪 Примеры использования:
        cat /proc/cpuinfo         # характеристики CPU
        cat /proc/meminfo         # состояние оперативной памяти
        ls /proc/$$               # информация о текущем bash-процессе
        echo 1 > /proc/sys/net/ipv4/ip_forward  # включить IP-форвардинг
        /proc/$$/wchan # текущая функция, в которой находится процесс
        /proc/$$/cmdline # команда, с которой запущен процесс
        /proc/$$/environ # окружение процесса (переменные среды)
        ls -la /proc/*/fd 2> /dev/null  | grep -i deleted # список открытых файлов с пометкой deleted
        ls -la /proc/*/fdinfo 2> /dev/null # информация о файловых дескрипторах процессов
 
/sys    виртуальная файловая система, представляющая информацию о устройствах и драйверах ядра.
        /sys/class/ — классы устройств (например, сетевые интерфейсы, USB-устройства).
        /sys/block/ — блочные устройства (жесткие диски, SSD).
        Позволяет управлять оборудованием на низком уровне.

        🧪 Примеры использования:
        cat /sys/class/net/eth0/address    # MAC-адрес интерфейса
        cat /sys/block/sda/size            # размер диска в 512-блоках
        echo 0 > /sys/class/leds/input3::capslock/brightness  # выключить CapsLock светодиод



/dev    каталог, содержащий файлы устройств (device files).
        /dev/null — специальный файл, который отбрасывает все данные, записанные в него.
        /dev/zero — специальный файл, который генерирует нулевые байты.
        /dev/random и /dev/urandom — генераторы случайных чисел.
        /dev/sda, /dev/sdb и т.д. — блочные устройства (жесткие диски, SSD).
        /dev/tty* — терминальные устройства (консоли, виртуальные терминалы).

        🧪 Примеры использования:
        dd if=/dev/zero of=zeroes.bin bs=1M count=10     # создаёт файл из нулей
        cat /dev/urandom | head -c 16 | base64           # сгенерировать случайную строку
        echo "test" > /dev/tty                           # вывести на терминал


Практика:
        # восстановление удалённого файла из дескриптора
        echo "Hello, World!" > /tmp/testfile.txt
        ls -la /proc/*/fd 2> /dev/null | grep deleted
        cat /proc/$$/fd/3 > /tmp/recovered_file.txt

        # освобождение места на диске после удаления файла но с открытым дескриптором
        1 - "можно завершить процесс, который держит дескриптор открытым"
        2 - "> /proc/PID/fd/4" # сам файл остаётся на диске, но будет удалён после закрытия дескриптора

        # сбросить кеш памяти
        echo 3 > /proc/sys/vm/drop_caches  # сброс кеша страниц, dentries и inodes

        # просмотр параметров ядра
        # (тюнинг через sysctl)
        sysctl -a
        ls -la /proc/sys/